<div class="panel panel-default">
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <%= link_to 'ショッピングサイト', root_path, class: 'navbar-brand' %>

          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#gnav">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%# <div class="collapse navbar-collapse" id="#gnav">
          </div> %>
          <%# end examine %>
        </div>
        <div class="collapse navbar-collapse" id="gnav">
          <!-- 検索フォーム -->
          <!-- <form class="navbar-form navbar-left nav-search" role="search"> -->
            <!-- <div class="form-group"> -->
            <div class="hidden-xs">
              <%= search_form_for @q, url: search_tops_path, html:{class: 'navbar-form navbar-left nav-search', role:"search"} do |f|%>
                <!-- <div class="row col-md-12"> -->
                <%= f.search_field :itemName_or_itemCodeNo_or_price_or_genre_id_cont, placeholder: '商品名 品番 価格 ジャンルで検索できます', class: 'form-control' %>
                <!-- </div> -->
                <%= f.submit "検索", class: "btn btn-default" %>
              <% end %>
            </div>
            <!-- </div> -->

          <!-- </form> -->
          <ul class="nav navbar-nav navbar-right">
            <% unless user_signed_in? %>
            <%# not sign in %>
              <li><%= link_to '会員登録', new_user_registration_path, class: 'text-center' %></li>
              <li><%= link_to 'ログイン', new_user_session_path, class: 'text-center' %></li>
            <% else %>
            <%# already sign in %>
            <li class="hidden-xs">
              <%= link_to(cartitems_path, id: 'no-padding') do %>
                <div id="cart-count">
                  <%= image_tag 'dog_cart.png', size: '50x50'%>
                  <p>
                    <%= @cartitems.sum(:quantity) %>
                  </p>
                </div>
              <% end %>
            </li>
            <li>
              <%= link_to "カートに入っている商品 (#{@cartitems.sum(:quantity)})", cartitems_path, class: 'text-center visible-xs' %>
            </li>
            <li><%= link_to '注文履歴', orders_path, class: 'text-center' %></li>
            <li><%= link_to 'ユーザー情報', settings_users_dashboard_path, class: 'text-center' %></li>
            <li><%= link_to 'Likeリスト', likes_path, class: 'text-center' %></li>
            <li><%= link_to 'ログアウト', destroy_user_session_path, class: 'text-center' %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
</div>
